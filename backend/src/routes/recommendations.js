import {Router} from 'express'; import {auth} from '../middleware/auth.js'; const r=Router(); r.get('/',auth,(req,res)=>res.json([{id:1,title:'Check overnight flow',text:'A healthy home should have near-zero flow when all taps are closed.',impact:'High'},{id:2,title:'Fix dripping fixtures',text:'A slow drip can waste hundreds of litres over time.',impact:'Medium'},{id:3,title:'Use a water schedule',text:'Run high-consumption activities during planned time windows.',impact:'Medium'},{id:4,title:'Review weekly peaks',text:'Compare unusual spikes with your household routine before assuming a leak.',impact:'Low'}])); export default r;
