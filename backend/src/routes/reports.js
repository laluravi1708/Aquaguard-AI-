import {Router} from 'express'; import {auth} from '../middleware/auth.js'; const r=Router(); r.get('/usage',auth,(req,res)=>{const rs=req.app.locals.readings||[]; const map={}; rs.forEach(x=>{const d=new Date(x.timestamp).toLocaleDateString('en-IN',{day:'2-digit',month:'short'}); map[d]=(map[d]||0)+Number(x.liters)}); res.json(Object.entries(map).map(([date,liters])=>({date,liters:Math.round(liters)})));}); export default r;
