const BASE='http://localhost:5000/api';
async function request(path,options={}){const token=localStorage.getItem('aq_token'); const r=await fetch(BASE+path,{...options,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{})}}); const data=await r.json(); if(!r.ok) throw new Error(data.message||'Request failed'); return data;}
export const api={login:(body)=>request('/auth/login',{method:'POST',body:JSON.stringify(body)}),summary:()=>request('/dashboard/summary'),readings:()=>request('/readings'),simulate:(count=24)=>request('/readings/simulate',{method:'POST',body:JSON.stringify({count})}),alerts:()=>request('/alerts'),resolve:(id)=>request(`/alerts/${id}/resolve`,{method:'PATCH'}),recommendations:()=>request('/recommendations'),report:()=>request('/reports/usage')};
